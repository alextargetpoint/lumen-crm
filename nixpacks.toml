# Railway/Nixpacks: доустановить ffmpeg (сжатие видео-креативов) + poppler-utils
# (pdfimages/pdftoppm — извлечение фото/рендеров из PDF-брошюр любой кодировки).
# aptPkgs дополняет базовый setup (node ставится автоматически), не заменяя его.
[phases.setup]
aptPkgs = ["ffmpeg", "poppler-utils"]
